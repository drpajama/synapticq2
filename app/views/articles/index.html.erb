<h1>문제 Pool</h1>

<table class="table">
  <tr>
    <th>Subject</th>
    <th>Type</th>
    <th>Summary</th>
    <th>SynapticMed Topic</th>
    <th>Actions</th>
  </tr>

  <% @articles.each do |article| %>
    <tr>
      <td><%= article_subject(article) %></td>
      <td><%= article.qtype %></td>
      <td><%= article.summary %></td>
      <td><%= article.topic %></td>
      <td>
        <%= link_to icon('eye-open'), article %>&nbsp;
        <% if user_signed_in? %>
          <%= link_to icon('edit'), edit_article_path(article) %>&nbsp;
          <%= link_to icon('trash'), article, method: :delete, data: { confirm: 'Are you sure?' }  %>
        <% end %>
      </td>
    </tr>
  <% end %>

</table>
<%= link_to 'Add new article', new_article_path, class: 'btn btn-default' %>
